
Steps to install eslint
1. Install eslint
npm install --save-dev eslint
2. Initialize eslint
eslint --init
//gives error - command not found: eslint
//install higher version of node node 13+ 
or install lower version of eslint

2. In your project folder run 
./node_modules/.bin/eslint --init 
which worked for me

3. use refactor , codestyle
